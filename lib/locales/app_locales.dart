/// Mirrors web `src/locales/app_locales.ts` + `en.json` for the keys the
/// auth flow actually uses.
class AppLocales {
  AppLocales._();

  static const signInTitle = 'Sign In';
  static const signInButton = 'Sign In';
  static const signInGoogleFailure = 'Google authentication failed!';
  static const signInGoogleButton = 'Sign in with Google';
  static const signOutButton = 'Sign Out';
  static const home = 'Home';
  static const goBack = 'Go Back';
  static const submit = 'Submit';
}
